<!--
    Ce fichier contient des styles XAML qui simplifient le développement d'applications.

    En plus d'être pratiques, ils sont aussi requis par la plupart des modèles de projet et d'élément Visual Studio.
    Si vous supprimez, renommez ou modifiez de toute autre façon le contenu de ces fichiers, il est possible qu'un projet
    ne soit pas généré ou qu'il ne se génère pas lors de l'ajout de pages supplémentaires. Si vous voulez modifier ces
    styles, il est recommandé d'en copier le contenu sous un autre nom, puis de modifier votre
    copie privée.
-->

<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
    xmlns:Converter="clr-namespace:AppPublication.Tools.Converter">

    <Style x:Key="TrailSpinnerStyle" TargetType="Control">
        <!-- <Setter Property="Foreground" Value="#007ACC" /> -->
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Control">
                    <Grid x:Name="Root" 
                      Background="Transparent"
                      RenderTransformOrigin="0.5, 0.5">

                        <Grid.RenderTransform>
                            <RotateTransform x:Name="SpinnerRotate" Angle="0" />
                        </Grid.RenderTransform>

                        <Viewbox>
                            <Canvas Width="100" Height="100">
                                <Ellipse Width="20" Height="20" Fill="{TemplateBinding Foreground}" Opacity="1.0" Canvas.Left="40" Canvas.Top="0"/>

                                <Ellipse Width="20" Height="20" Fill="{TemplateBinding Foreground}" Opacity="0.85" Canvas.Left="40" Canvas.Top="0">
                                    <Ellipse.RenderTransform>
                                        <RotateTransform Angle="45" CenterX="10" CenterY="50" />
                                    </Ellipse.RenderTransform>
                                </Ellipse>

                                <Ellipse Width="20" Height="20" Fill="{TemplateBinding Foreground}" Opacity="0.70" Canvas.Left="40" Canvas.Top="0">
                                    <Ellipse.RenderTransform>
                                        <RotateTransform Angle="90" CenterX="10" CenterY="50" />
                                    </Ellipse.RenderTransform>
                                </Ellipse>

                                <Ellipse Width="20" Height="20" Fill="{TemplateBinding Foreground}" Opacity="0.55" Canvas.Left="40" Canvas.Top="0">
                                    <Ellipse.RenderTransform>
                                        <RotateTransform Angle="135" CenterX="10" CenterY="50" />
                                    </Ellipse.RenderTransform>
                                </Ellipse>

                                <Ellipse Width="20" Height="20" Fill="{TemplateBinding Foreground}" Opacity="0.40" Canvas.Left="40" Canvas.Top="0">
                                    <Ellipse.RenderTransform>
                                        <RotateTransform Angle="180" CenterX="10" CenterY="50" />
                                    </Ellipse.RenderTransform>
                                </Ellipse>

                                <Ellipse Width="20" Height="20" Fill="{TemplateBinding Foreground}" Opacity="0.25" Canvas.Left="40" Canvas.Top="0">
                                    <Ellipse.RenderTransform>
                                        <RotateTransform Angle="225" CenterX="10" CenterY="50" />
                                    </Ellipse.RenderTransform>
                                </Ellipse>

                                <Ellipse Width="20" Height="20" Fill="{TemplateBinding Foreground}" Opacity="0.15" Canvas.Left="40" Canvas.Top="0">
                                    <Ellipse.RenderTransform>
                                        <RotateTransform Angle="270" CenterX="10" CenterY="50" />
                                    </Ellipse.RenderTransform>
                                </Ellipse>

                                <Ellipse Width="20" Height="20" Fill="{TemplateBinding Foreground}" Opacity="0.05" Canvas.Left="40" Canvas.Top="0">
                                    <Ellipse.RenderTransform>
                                        <RotateTransform Angle="315" CenterX="10" CenterY="50" />
                                    </Ellipse.RenderTransform>
                                </Ellipse>
                            </Canvas>
                        </Viewbox>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    <DoubleAnimation Storyboard.TargetName="SpinnerRotate"
                                                 Storyboard.TargetProperty="Angle"
                                                 From="360" To="0" 
                                                 Duration="0:0:1.0" 
                                                 RepeatBehavior="Forever" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BusyIconStyle" TargetType="Control">
        <Setter Property="Width" Value="20"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Control">
                    <Image x:Name="PART_SpinImage"
                           Source="pack://application:,,,/Tools;component/data/images/icons/black/refresh-32.png"
                           RenderTransformOrigin="0.5, 0.5"
                           Visibility="Collapsed">
                        <Image.RenderTransform>
                            <RotateTransform Angle="0"/>
                        </Image.RenderTransform>
                    </Image>

                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" Value="True">
                            <Setter TargetName="PART_SpinImage" Property="Visibility" Value="Visible"/>
                            <DataTrigger.EnterActions>
                                <BeginStoryboard x:Name="SpinStoryboard">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="PART_SpinImage"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                         From="0" To="360" Duration="0:0:1" RepeatBehavior="Forever"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                            <DataTrigger.ExitActions>
                                <RemoveStoryboard BeginStoryboardName="SpinStoryboard"/>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="GridViewStyle1" TargetType="{x:Type telerik:RadGridView}">
        <Setter Property="VerticalAlignment" Value="Top" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="ReorderColumnsMode" Value="ReorderColumns" />
        <Setter Property="GroupPanelBackground" Value="Transparent" />
        <Setter Property="GroupPanelForeground" Value="white" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="RowIndicatorVisibility" Value="Collapsed" />
        <Setter Property="AutoGenerateColumns" Value="False" />
        <Setter Property="GridLinesVisibility" Value="Vertical" />
        <Setter Property="AutoExpandGroups" Value="True" />
        <Setter Property="ShowGroupPanel" Value="False" />
        <Setter Property="IsFilteringAllowed" Value="False" />
        <Setter Property="telerik:StyleManager.Theme" Value="Expression_Dark" />
        <Setter Property="Margin" Value="10"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="IsSynchronizedWithCurrentItem" Value="False"/>
        <Setter Property="CanUserSelect" Value="True"/>
        <Setter Property="ActionOnLostFocus" Value="CancelEdit"/>
        <Setter Property="EditTriggers" Value="CellClick"/>
        <Setter Property="ShowColumnFooters" Value="False"/>
        <Setter Property="KeyboardNavigation.IsTabStop" Value="False"/>
    </Style>

    <Style x:Key="EpreuveBorder2" TargetType="{x:Type Border}">
        <Setter Property="BorderBrush" Value="Black" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="5" />
        <Setter Property="Background" Value="#DDDDDD" />
        <Setter Property="Opacity" Value="0.7" />
    </Style>

    <Style x:Key="TextGridStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{telerik:Windows8Resource ResourceKey=FontFamily}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Margin" Value="5" />
    </Style>

    <Style x:Key="Header1Style" TargetType="{x:Type StackPanel}">
        <Setter Property="Height" Value="30" />
        <!--<Setter Property="Background" Value="{telerik:Windows8Resource ResourceKey=AccentBrush}" />-->
        <Setter Property="Background" Value="#333333" />
        <Setter Property="Orientation" Value="Horizontal" />
        <Setter Property="Margin" Value="0" />
    </Style>

    <Style x:Key="Header1TextStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{telerik:Windows8Resource ResourceKey=FontFamily}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Margin" Value="5 5 5 5" />
    </Style>

    <Style x:Key="ContentTextStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{telerik:Windows8Resource ResourceKey=FontFamily}" />
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Margin" Value="5 10" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="TextBoxTextStyle" TargetType="{x:Type TextBlock}">
    <Setter Property="FontFamily" Value="{telerik:Windows8Resource ResourceKey=FontFamily}" />
    <Setter Property="FontWeight" Value="Normal" />
    <Setter Property="VerticalAlignment" Value="Stretch" />
    <Setter Property="HorizontalAlignment" Value="Center" />
    <Setter Property="FontSize" Value="14" />
    <Setter Property="Background" Value="White" />
    <Setter Property="Margin" Value="5 10" />
    <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="LabelStyle" TargetType="Label">
        <Setter Property="Foreground" Value="#EEEEEE" />
        <Setter Property="FontSize" Value="14" />
        <!-- <Setter Property="FontFamily"  Value="pack://application:,,,/Tools;component/Styles/Polices/#Cooper Black"/>  -->
        <Setter Property="FontFamily" Value="{telerik:Windows8Resource ResourceKey=FontFamily}" />
        <Setter Property="Margin"  Value="5"/>
        <Setter Property="VerticalAlignment"  Value="Center"/>
        <Setter Property="HorizontalAlignment"  Value="Left"/>
    </Style>

    <Style x:Key="ButtonLabelStyle" TargetType="Label">
        <Setter Property="Foreground" Value="#EEEEEE" />
        <Setter Property="FontSize" Value="12" />
        <!-- <Setter Property="FontFamily"  Value="pack://application:,,,/Tools;component/Styles/Polices/#Cooper Black"/>  -->
        <Setter Property="FontFamily" Value="{telerik:Windows8Resource ResourceKey=FontFamily}" />
        <Setter Property="VerticalAlignment"  Value="Center"/>
        <Setter Property="HorizontalAlignment"  Value="Left"/>
    </Style>

    <Style x:Key="DropDownStyle" TargetType="{x:Type telerik:RadComboBox}">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontFamily" Value="{telerik:Windows8Resource ResourceKey=FontFamily}" />
        <Setter Property="Margin"  Value="5"/>
        <Setter Property="VerticalAlignment"  Value="Center"/>
        <Setter Property="HorizontalAlignment"  Value="Left"/>
    </Style>

    <Style x:Key="CheckboxStyle" TargetType="CheckBox">
        <Setter Property="Foreground" Value="#EEEEEE" />
        <Setter Property="FontSize" Value="14" />
        <!-- <Setter Property="FontFamily"  Value="pack://application:,,,/Tools;component/Styles/Polices/#Cooper Black"/> -->
        <Setter Property="FontFamily" Value="{telerik:Windows8Resource ResourceKey=FontFamily}" />
        <Setter Property="Margin"  Value="5"/>
        <Setter Property="VerticalAlignment"  Value="Center"/>
        <Setter Property="HorizontalAlignment"  Value="Left"/>
    </Style>

    <Style x:Key="LabelStyleValue" TargetType="Label">
        <Setter Property="Foreground" Value="#EEEEEE" />
        <Setter Property="FontSize" Value="12" />
        <!-- <Setter Property="FontFamily"  Value="pack://application:,,,/Tools;component/Styles/Polices/#Cabin"/> -->
        <Setter Property="FontFamily" Value="{telerik:Windows8Resource ResourceKey=FontFamily}" />
        <Setter Property="Margin"  Value="5"/>
        <Setter Property="FontStyle"  Value="Italic"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="VerticalAlignment"  Value="Center"/>
        <Setter Property="HorizontalAlignment"  Value="Left"/>
    </Style>

    <Style x:Key="HeaderStyle" TargetType="Label">
        <Setter Property="Foreground" Value="#EEEEEE" />
        <Setter Property="FontSize" Value="14" />
        <!-- <Setter Property="FontFamily"  Value="pack://application:,,,/Tools;component/Styles/Polices/#Cooper Black"/> -->
        <Setter Property="FontFamily" Value="{telerik:Windows8Resource ResourceKey=FontFamily}" />
        <Setter Property="Margin"  Value="5"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="VerticalAlignment"  Value="Center"/>
        <Setter Property="HorizontalAlignment"  Value="Left"/>
    </Style>

    <Style x:Key="RadButtonStyleEdit" TargetType="telerik:RadButton">
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <Setter Property="Margin" Value="5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Grid>
                        <Rectangle RadiusX="5" RadiusY="5" Grid.RowSpan="4" Fill="#FF1E93ED" Margin="0" Height="25" Width="25">
                            <Rectangle.Effect>
                                <DropShadowEffect BlurRadius="7" ShadowDepth="0" Color="white" Opacity="1"/>
                            </Rectangle.Effect>
                        </Rectangle>
                        <WrapPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Image Source="{Binding Content, RelativeSource={RelativeSource FindAncestor,AncestorType={x:Type Button}}}" Height="15" Width="15" Margin="5" ToolTip="{TemplateBinding ToolTip}"/>
                        </WrapPanel>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Cursor" Value="Hand" />
            </Trigger>
            <EventTrigger RoutedEvent="Mouse.MouseEnter">
                <BeginStoryboard>
                    <Storyboard TargetProperty="RenderTransform.ScaleX">
                        <DoubleAnimation To="0.9" Duration="0:0:0.1" />
                    </Storyboard>
                </BeginStoryboard>
                <BeginStoryboard>
                    <Storyboard TargetProperty="RenderTransform.ScaleY">
                        <DoubleAnimation To="0.9" Duration="0:0:0.1" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>

            <EventTrigger RoutedEvent="Mouse.MouseLeave">
                <BeginStoryboard>
                    <Storyboard TargetProperty="RenderTransform.ScaleX">
                        <DoubleAnimation To="1.0" Duration="0:0:0.1" />
                    </Storyboard>
                </BeginStoryboard>
                <BeginStoryboard>
                    <Storyboard TargetProperty="RenderTransform.ScaleY">
                        <DoubleAnimation To="1.0" Duration="0:0:0.1" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>
    
    <Style x:Key="RadButtonStyle1" TargetType="telerik:RadButton">
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Grid>
                        <Rectangle RadiusX="5" RadiusY="5" Grid.RowSpan="4" Fill="#FF1E93ED" Margin="0">
                            <Rectangle.Effect>
                                <DropShadowEffect BlurRadius="7" ShadowDepth="0" Color="white" Opacity="1"/>
                            </Rectangle.Effect>
                        </Rectangle>
                        <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
                            <Image Source="{Binding Tag, RelativeSource={RelativeSource FindAncestor,AncestorType={x:Type Button}}}" Height="25" Margin="5" ToolTip="{TemplateBinding ToolTip}"/>
                        </WrapPanel>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Cursor" Value="Hand" />
            </Trigger>
            <EventTrigger RoutedEvent="Mouse.MouseEnter">
                <BeginStoryboard>
                    <Storyboard TargetProperty="RenderTransform.ScaleX">
                        <DoubleAnimation To="0.9" Duration="0:0:0.1" />
                    </Storyboard>
                </BeginStoryboard>
                <BeginStoryboard>
                    <Storyboard TargetProperty="RenderTransform.ScaleY">
                        <DoubleAnimation To="0.9" Duration="0:0:0.1" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>

            <EventTrigger RoutedEvent="Mouse.MouseLeave">
                <BeginStoryboard>
                    <Storyboard TargetProperty="RenderTransform.ScaleX">
                        <DoubleAnimation To="1.0" Duration="0:0:0.1" />
                    </Storyboard>
                </BeginStoryboard>
                <BeginStoryboard>
                    <Storyboard TargetProperty="RenderTransform.ScaleY">
                        <DoubleAnimation To="1.0" Duration="0:0:0.1" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>

        </Style.Triggers>
    </Style>


    <Style x:Key="Header2TextStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontFamily" Value="{telerik:Windows8Resource ResourceKey=FontFamily}" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="Foreground" Value="Black" />
        <Setter Property="Margin" Value="5 5" />
    </Style>

    <Style TargetType="Rectangle" x:Key="AnimatedStatusStyle">
        <Setter Property="Height" Value="15"/>
        <Setter Property="Width" Value="15"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Stroke" Value="Transparent"/>
        <Setter Property="Opacity" Value="1.0"/>

        <Setter Property="Fill" Value="Red"/>

        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="True">

                <Setter Property="Fill" Value="LimeGreen"/>

                <DataTrigger.EnterActions>
                    <BeginStoryboard x:Name="BlinkStoryboard">
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             From="1.0" To="0.2" 
                                             Duration="0:0:0.8"
                                             AutoReverse="True" 
                                             RepeatBehavior="Forever"/>
                        </Storyboard>
                    </BeginStoryboard>
                </DataTrigger.EnterActions>

                <DataTrigger.ExitActions>
                    <RemoveStoryboard BeginStoryboardName="BlinkStoryboard"/>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1.0" Duration="0:0:0.1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </DataTrigger.ExitActions>

            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Image" x:Key="InfoTooltipStyle">
        <Style.Setters>
            <Setter Property="ToolTip" Value="{Binding Path=MessageDetaille}"/>
        </Style.Setters>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Path=MessageDetaille}" Value="">
                <Setter Property="Visibility" Value="Hidden" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=MessageDetaille}" Value="{x:Null}">
                <Setter Property="Visibility" Value="Hidden" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!--       
        Stopped = -1,   // Le site est arrete
        Listening = 0,  // Le site local est demarre (en ecoute)
        Idle = 1,       // site distant est demarre mais en attente
        Syncing = 2,    // site distant en cours de synchronisation
        Cleaning = 3    // site distant en cours de nettoyage
    -->

    <Style TargetType="telerik:RadProgressBar" x:Key="StatusProgressBarStyle">
        <Style.Setters>
            <Setter Property="Value" Value="{Binding Path=Progress}"/>
            <Setter Property="Visibility" Value="Collapsed"/>
            <Setter Property="IsIndeterminate" Value="{Binding Path=IsProgressUnknown}"/> 
        </Style.Setters>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Path=State}" Value="2">
                <Setter Property="Visibility" Value="Visible" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=State}" Value="3">
                <Setter Property="Visibility" Value="Visible" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Label" x:Key="StatusGenerationSiteProgressLabelStyle">
        <Style.Setters>
            <Setter Property="Foreground" Value="#EEEEEE" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontFamily" Value="{telerik:Windows8Resource ResourceKey=FontFamily}" />
            <Setter Property="FontStyle"  Value="Italic"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Content" Value="{Binding Path=Progress}"/>
            <Setter Property="ContentStringFormat" Value="({0}%)"/>
            <Setter Property="Visibility" Value="Visible"/>
        </Style.Setters>
        <!-- <Style.Triggers>
            <DataTrigger Binding="{Binding Path=State}" Value="2">
                <Setter Property="Visibility" Value="Visible" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=State}" Value="3">
                <Setter Property="Visibility" Value="Visible" />
            </DataTrigger>
        </Style.Triggers> -->
        <Style.Triggers>
            <DataTrigger Binding="{Binding Path=Progress}" Value="-1">
                <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Label" x:Key="StatusMiniSiteProgressLabelStyle">
        <Style.Setters>
            <Setter Property="Foreground" Value="#EEEEEE" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontFamily" Value="{telerik:Windows8Resource ResourceKey=FontFamily}" />
            <Setter Property="FontStyle"  Value="Italic"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Content" Value="{Binding Path=Progress}"/>
            <Setter Property="ContentStringFormat" Value="({0}%)"/>
            <Setter Property="Visibility" Value="Visible"/>
        </Style.Setters>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Path=Progress}" Value="-1">
                <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
        </Style.Triggers>
    </Style>


    <Style TargetType="Ellipse" x:Key="OperationStatusStyle">
        <Style.Setters>
            <Setter Property="Stroke" Value="White"/>
            <Setter Property="Fill" Value="DarkGray"/>
        </Style.Setters>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Path=State}" Value="2">
                <Setter Property="Fill" Value="Orange"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=State}" Value="3">
                <Setter Property="Fill" Value="BlueViolet"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>


    <DataTemplate x:Key="ServerItemTemplate">
        <StackPanel Orientation="Horizontal">
            <Label Content="{Binding IEP}">
            </Label>
            <Label Content="{Binding machine}"></Label>
            <Label Content="{Binding user}"></Label>
            <Label Content="{Binding competition}"></Label>
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>
