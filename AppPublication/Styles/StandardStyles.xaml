<!--
    Ce fichier contient des styles XAML qui simplifient le développement d'applications.

    En plus d'être pratiques, ils sont aussi requis par la plupart des modèles de projet et d'élément Visual Studio.
    Si vous supprimez, renommez ou modifiez de toute autre façon le contenu de ces fichiers, il est possible qu'un projet
    ne soit pas généré ou qu'il ne se génère pas lors de l'ajout de pages supplémentaires. Si vous voulez modifier ces
    styles, il est recommandé d'en copier le contenu sous un autre nom, puis de modifier votre
    copie privée.
-->

<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:hc="https://handyorg.github.io/handycontrol"
    xmlns:Converter="clr-namespace:AppPublication.Tools.Converter">

    <Style TargetType="Button" BasedOn="{StaticResource ButtonDefault}">
        <Setter Property="hc:BorderElement.CornerRadius" Value="2" />
        <Setter Property="MinHeight" Value="26" />
        <Setter Property="Padding" Value="12,2" />
        <Setter Property="Background" Value="#575859" />
        <Setter Property="BorderBrush" Value="#A1A1A1" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="#FFFFFF" />

        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#878787" />
                <Setter Property="BorderBrush" Value="#A1A1A1" />
            </Trigger>

            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#424242" />
                <Setter Property="BorderBrush" Value="#202020" />
            </Trigger>

            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.4" />
                </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="BusyIconStyle" TargetType="Control">
        <Setter Property="Width" Value="20"/>
        <Setter Property="Height" Value="20"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Control">
                    <Image x:Name="PART_SpinImage"
                           Source="pack://application:,,,/FranceJudo.UI;component/Wpf/Resources/Media/Icons/black/refresh-32.png"
                           RenderTransformOrigin="0.5, 0.5"
                           Visibility="Collapsed">
                        <Image.RenderTransform>
                            <RotateTransform Angle="0"/>
                        </Image.RenderTransform>
                    </Image>

                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" Value="True">
                            <Setter TargetName="PART_SpinImage" Property="Visibility" Value="Visible"/>
                            <DataTrigger.EnterActions>
                                <BeginStoryboard x:Name="SpinStoryboard">
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="PART_SpinImage"
                                                         Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                         From="0" To="360" Duration="0:0:1" RepeatBehavior="Forever"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </DataTrigger.EnterActions>
                            <DataTrigger.ExitActions>
                                <RemoveStoryboard BeginStoryboardName="SpinStoryboard"/>
                            </DataTrigger.ExitActions>
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="GridViewStyle1" TargetType="{x:Type DataGrid}" BasedOn="{StaticResource {x:Type DataGrid}}">

        <Setter Property="VerticalAlignment" Value="Top" />
        <Setter Property="HorizontalAlignment" Value="Left" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="#404040" />
        <Setter Property="HeadersVisibility" Value="Column" />
        <Setter Property="AutoGenerateColumns" Value="False" />
        <Setter Property="GridLinesVisibility" Value="Vertical" />
        <Setter Property="Margin" Value="10"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="IsSynchronizedWithCurrentItem" Value="False"/>
        <Setter Property="KeyboardNavigation.IsTabStop" Value="False"/>

        <Setter Property="RowHeight" Value="28" />
        <Setter Property="Padding" Value="2" />

        <Setter Property="Background" Value="#2B2B2B" />
        <Setter Property="RowBackground" Value="#2B2B2B" />
        <Setter Property="AlternatingRowBackground" Value="#333333" />

        <Setter Property="VerticalGridLinesBrush" Value="#404040" />
        <Setter Property="HorizontalGridLinesBrush" Value="#404040" />

        <Setter Property="hc:BorderElement.CornerRadius" Value="0" />

        <Setter Property="ColumnHeaderStyle">
            <Setter.Value>
                <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource {x:Type DataGridColumnHeader}}">
                    <Setter Property="Background" Value="#222222" />
                    <Setter Property="Foreground" Value="#CCCCCC" />
                    <Setter Property="FontWeight" Value="SemiBold" />
                    <Setter Property="Height" Value="30" />
                    <Setter Property="Padding" Value="5 2 5 2" />

                    <Setter Property="hc:BorderElement.CornerRadius" Value="0" />
                    <Setter Property="BorderBrush" Value="#404040" />
                    <Setter Property="BorderThickness" Value="0,0,1,1" />
                </Style>
            </Setter.Value>
        </Setter>

        <Setter Property="CellStyle">
            <Setter.Value>
                <Style TargetType="DataGridCell" BasedOn="{StaticResource {x:Type DataGridCell}}">
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="Padding" Value="2" />
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}" 
                                    BorderThickness="0">
                                    <ContentPresenter VerticalAlignment="Center" Margin="{TemplateBinding Padding}" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>

    </Style>

    <Style x:Key="StatTabItemStyle" TargetType="TabItem">
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Background" Value="{DynamicResource RegionBrush}"/>
        <Setter Property="Foreground" Value="DimGray"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Grid Background="{TemplateBinding Background}" MinWidth="120" MinHeight="35">

                        <TextBlock Text="{TemplateBinding Header}" 
                               Foreground="{TemplateBinding Foreground}"
                               FontWeight="{TemplateBinding FontWeight}"
                               HorizontalAlignment="Center" 
                               VerticalAlignment="Center"/>

                        <Border x:Name="Liseret" Height="2" Background="White" 
                            VerticalAlignment="Bottom" Visibility="Collapsed"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource RegionBrush}"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter TargetName="Liseret" Property="Visibility" Value="Visible"/>
                        </Trigger>

                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsSelected" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter Property="Background" Value="#333333"/>
                            <Setter Property="Foreground" Value="LightGray"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="EpreuveBorder2" TargetType="{x:Type Border}">
        <Setter Property="BorderBrush" Value="Black" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="5" />
        <Setter Property="Background" Value="#DDDDDD" />
        <Setter Property="Opacity" Value="0.7" />
    </Style>

    <Style x:Key="TextGridStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Margin" Value="5" />
    </Style>

    <Style x:Key="Header1Style" TargetType="{x:Type StackPanel}">
        <Setter Property="Height" Value="30" />
        <Setter Property="Background" Value="#333333" />
        <Setter Property="Orientation" Value="Horizontal" />
        <Setter Property="Margin" Value="0" />
    </Style>

    <Style x:Key="Header1TextStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Margin" Value="5 5 5 5" />
    </Style>

    <Style x:Key="ContentTextStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontWeight" Value="Normal" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Margin" Value="5 10" />
        <Setter Property="TextWrapping" Value="Wrap" />
    </Style>

    <Style x:Key="TextBoxTextStyle" TargetType="{x:Type TextBlock}">
    <Setter Property="FontWeight" Value="Normal" />
    <Setter Property="VerticalAlignment" Value="Stretch" />
    <Setter Property="HorizontalAlignment" Value="Center" />
    <Setter Property="FontSize" Value="14" />
    <Setter Property="Background" Value="White" />
    <Setter Property="Margin" Value="5 10" />
    <Setter Property="TextTrimming" Value="CharacterEllipsis" />
     <Setter Property="Foreground" Value="DarkSlateGray" />
    </Style>

    <Style x:Key="LabelErrorStyle" TargetType="Label">
        <Setter Property="Foreground" Value="RosyBrown" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="FontStyle" Value="Italic" />
        <Setter Property="VerticalAlignment"  Value="Center"/>
        <Setter Property="HorizontalAlignment"  Value="Left"/>
    </Style>

    <Style x:Key="LabelStyle" TargetType="Label">
        <Setter Property="Foreground" Value="#EEEEEE" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Margin"  Value="5"/>
        <Setter Property="VerticalAlignment"  Value="Center"/>
        <Setter Property="HorizontalAlignment"  Value="Left"/>
    </Style>

    <Style x:Key="ButtonLabelStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="#EEEEEE" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="VerticalAlignment"  Value="Center"/>
        <Setter Property="HorizontalAlignment"  Value="Left"/>
    </Style>

    <Style x:Key="DropDownStyle" TargetType="{x:Type ComboBox}" BasedOn="{StaticResource {x:Type ComboBox}}">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Margin"  Value="5"/>
        <Setter Property="VerticalAlignment"  Value="Center"/>
        <Setter Property="HorizontalAlignment"  Value="Left"/>
    </Style>

    <Style x:Key="CheckboxStyle" TargetType="CheckBox">
        <Setter Property="Foreground" Value="#EEEEEE" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Margin"  Value="5"/>
        <Setter Property="VerticalAlignment"  Value="Center"/>
        <Setter Property="HorizontalAlignment"  Value="Left"/>
    </Style>

    <Style x:Key="LabelStyleValue" TargetType="Label">
        <Setter Property="Foreground" Value="#EEEEEE" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="Margin"  Value="5"/>
        <Setter Property="FontStyle"  Value="Italic"/>
        <Setter Property="FontWeight" Value="Normal"/>
        <Setter Property="VerticalAlignment"  Value="Center"/>
        <Setter Property="HorizontalAlignment"  Value="Left"/>
    </Style>

    <Style x:Key="HeaderStyle" TargetType="Label">
        <Setter Property="Foreground" Value="#EEEEEE" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Margin"  Value="5"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="VerticalAlignment"  Value="Center"/>
        <Setter Property="HorizontalAlignment"  Value="Left"/>
    </Style>

    <Style x:Key="ButtonStyleEdit" TargetType="Button">
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <Setter Property="Margin" Value="5"/>
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1" />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate>
                    <Grid>
                        <Rectangle RadiusX="5" RadiusY="5" Grid.RowSpan="4" Fill="#FF1E93ED" Margin="0" Height="25" Width="25">
                            <Rectangle.Effect>
                                <DropShadowEffect BlurRadius="7" ShadowDepth="0" Color="white" Opacity="1"/>
                            </Rectangle.Effect>
                        </Rectangle>
                        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Image Source="{Binding Content, RelativeSource={RelativeSource FindAncestor,AncestorType={x:Type Button}}}" Height="15" Width="15" Margin="5" ToolTip="{TemplateBinding ToolTip}"/>
                        </Grid>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Cursor" Value="Hand" />
            </Trigger>
            <EventTrigger RoutedEvent="Mouse.MouseEnter">
                <BeginStoryboard>
                    <Storyboard TargetProperty="RenderTransform.ScaleX">
                        <DoubleAnimation To="0.9" Duration="0:0:0.1" />
                    </Storyboard>
                </BeginStoryboard>
                <BeginStoryboard>
                    <Storyboard TargetProperty="RenderTransform.ScaleY">
                        <DoubleAnimation To="0.9" Duration="0:0:0.1" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>

            <EventTrigger RoutedEvent="Mouse.MouseLeave">
                <BeginStoryboard>
                    <Storyboard TargetProperty="RenderTransform.ScaleX">
                        <DoubleAnimation To="1.0" Duration="0:0:0.1" />
                    </Storyboard>
                </BeginStoryboard>
                <BeginStoryboard>
                    <Storyboard TargetProperty="RenderTransform.ScaleY">
                        <DoubleAnimation To="1.0" Duration="0:0:0.1" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="ButtonStyleToolbar" TargetType="Button">
        <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="BorderBrush" Value="Transparent"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Width" Value="40" />
        <Setter Property="Height" Value="40" />
        <Setter Property="RenderTransform">
            <Setter.Value>
                <ScaleTransform ScaleX="1" ScaleY="1" />
            </Setter.Value>
        </Setter>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Grid x:Name="RootGrid" Background="Transparent">

                        <Rectangle x:Name="HaloRectangle" 
                               RadiusX="5" RadiusY="5" 
                               Fill="White" 
                               Opacity="0"
                               Margin="-1">
                            <Rectangle.Effect>
                                <DropShadowEffect BlurRadius="25" 
                                              ShadowDepth="0" 
                                              Color="White" 
                                              Opacity="1" 
                                              RenderingBias="Quality"/>
                            </Rectangle.Effect>
                        </Rectangle>

                        <Rectangle x:Name="BackgroundRectangle" 
                               RadiusX="5" RadiusY="5" 
                               Fill="#FF1E93ED" />

                        <ContentPresenter Content="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5"/>
                    </Grid>

                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="HaloRectangle" Property="Opacity" Value="1" />
                            <Setter Property="Cursor" Value="Hand" />
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="BackgroundRectangle" Property="Fill" Value="#4682B4" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <EventTrigger RoutedEvent="Mouse.MouseEnter">
                <BeginStoryboard>
                    <Storyboard TargetProperty="RenderTransform.ScaleX">
                        <DoubleAnimation To="0.97" Duration="0:0:0.1" />
                    </Storyboard>
                </BeginStoryboard>
                <BeginStoryboard>
                    <Storyboard TargetProperty="RenderTransform.ScaleY">
                        <DoubleAnimation To="0.97" Duration="0:0:0.1" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>

            <EventTrigger RoutedEvent="Mouse.MouseLeave">
                <BeginStoryboard>
                    <Storyboard TargetProperty="RenderTransform.ScaleX">
                        <DoubleAnimation To="1.0" Duration="0:0:0.1" />
                    </Storyboard>
                </BeginStoryboard>
                <BeginStoryboard>
                    <Storyboard TargetProperty="RenderTransform.ScaleY">
                        <DoubleAnimation To="1.0" Duration="0:0:0.1" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="Header2TextStyle" TargetType="{x:Type TextBlock}">
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Margin" Value="5 5" />
    </Style>

    <Style TargetType="Rectangle" x:Key="AnimatedStatusStyle">
        <Setter Property="Height" Value="15"/>
        <Setter Property="Width" Value="15"/>
        <Setter Property="Margin" Value="0"/>
        <Setter Property="Stroke" Value="Transparent"/>
        <Setter Property="Opacity" Value="1.0"/>

        <Setter Property="Fill" Value="Red"/>

        <Style.Triggers>
            <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=Tag}" Value="True">

                <Setter Property="Fill" Value="LimeGreen"/>

                <DataTrigger.EnterActions>
                    <BeginStoryboard x:Name="BlinkStoryboard">
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                             From="1.0" To="0.2" 
                                             Duration="0:0:0.8"
                                             AutoReverse="True" 
                                             RepeatBehavior="Forever"/>
                        </Storyboard>
                    </BeginStoryboard>
                </DataTrigger.EnterActions>

                <DataTrigger.ExitActions>
                    <RemoveStoryboard BeginStoryboardName="BlinkStoryboard"/>
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1.0" Duration="0:0:0.1"/>
                        </Storyboard>
                    </BeginStoryboard>
                </DataTrigger.ExitActions>

            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Image" x:Key="InfoTooltipStyle">
        <Style.Setters>
            <Setter Property="ToolTip" Value="{Binding Path=MessageDetaille}"/>
        </Style.Setters>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Path=MessageDetaille}" Value="">
                <Setter Property="Visibility" Value="Hidden" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=MessageDetaille}" Value="{x:Null}">
                <Setter Property="Visibility" Value="Hidden" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <!--       
        Stopped = -1,   // Le site est arrete
        Listening = 0,  // Le site local est demarre (en ecoute)
        Idle = 1,       // site distant est demarre mais en attente
        Syncing = 2,    // site distant en cours de synchronisation
        Cleaning = 3    // site distant en cours de nettoyage
    -->

    <Style TargetType="ProgressBar" x:Key="StatusProgressBarStyle" BasedOn="{StaticResource {x:Type ProgressBar}}">
        <Style.Setters>
            <Setter Property="hc:VisualElement.Text" Value=""/>
            <Setter Property="Value" Value="{Binding Path=Progress}"/>
            <Setter Property="Visibility" Value="Collapsed"/>
            <Setter Property="IsIndeterminate" Value="{Binding Path=IsProgressUnknown}"/> 
        </Style.Setters>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Path=State}" Value="2">
                <Setter Property="Visibility" Value="Visible" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=State}" Value="3">
                <Setter Property="Visibility" Value="Visible" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Label" x:Key="StatusGenerationSiteProgressLabelStyle">
        <Style.Setters>
            <Setter Property="Foreground" Value="#EEEEEE" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontStyle"  Value="Italic"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Content" Value="{Binding Path=Progress}"/>
            <Setter Property="ContentStringFormat" Value="({0}%)"/>
            <Setter Property="Visibility" Value="Visible"/>
        </Style.Setters>
        <!-- <Style.Triggers>
            <DataTrigger Binding="{Binding Path=State}" Value="2">
                <Setter Property="Visibility" Value="Visible" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=State}" Value="3">
                <Setter Property="Visibility" Value="Visible" />
            </DataTrigger>
        </Style.Triggers> -->
        <Style.Triggers>
            <DataTrigger Binding="{Binding Path=Progress}" Value="-1">
                <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Label" x:Key="StatusMiniSiteProgressLabelStyle">
        <Style.Setters>
            <Setter Property="Foreground" Value="#EEEEEE" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontStyle"  Value="Italic"/>
            <Setter Property="FontWeight" Value="Normal"/>
            <Setter Property="Content" Value="{Binding Path=Progress}"/>
            <Setter Property="ContentStringFormat" Value="({0}%)"/>
            <Setter Property="Visibility" Value="Visible"/>
        </Style.Setters>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Path=Progress}" Value="-1">
                <Setter Property="Visibility" Value="Collapsed" />
            </DataTrigger>
        </Style.Triggers>
    </Style>


    <Style TargetType="Ellipse" x:Key="OperationStatusStyle">
        <Style.Setters>
            <Setter Property="Stroke" Value="White"/>
            <Setter Property="Fill" Value="DarkGray"/>
        </Style.Setters>
        <Style.Triggers>
            <DataTrigger Binding="{Binding Path=State}" Value="2">
                <Setter Property="Fill" Value="Orange"/>
            </DataTrigger>
            <DataTrigger Binding="{Binding Path=State}" Value="3">
                <Setter Property="Fill" Value="BlueViolet"/>
            </DataTrigger>
        </Style.Triggers>
    </Style>


    <DataTemplate x:Key="ServerItemTemplate">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding IEP}" Style="{StaticResource TextGridStyle}"/>
            <TextBlock Text="{Binding Machine}" Style="{StaticResource TextGridStyle}"/>
            <TextBlock Text="{Binding User}" Style="{StaticResource TextGridStyle}"/>
            <TextBlock Text="{Binding Competition}" Style="{StaticResource TextGridStyle}"/>
        </StackPanel>
    </DataTemplate>
</ResourceDictionary>
